mp mkdir -p
rf rm -rf
xp chmod +x
cr cp -r

pa parus
pac sudo pacman -S
pay sudo pacman -Syy
par sudo pacman -R
paf sudo pacman -F

gc git clone --depth=1
gi git init
ga git add
gs git status
gm git commit -m
gp git push
gpl git pull

c code
n nvim
dup docker-compose up
dwn docker-compose down
dvn docker-compose down -v
